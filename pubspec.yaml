name: pinguteca_sdk_workspace
description: Monorepo workspace root for the Pinguteca Dart SDK family
publish_to: none

environment:
  sdk: ^3.12.0

workspace:
  - packages/core

dev_dependencies:
  melos: ^7.8.0
